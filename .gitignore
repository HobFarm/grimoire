# Environment & secrets
.env
.env.local
env.local
.dev.vars
.dev.vars.*

# Build artifacts
.wrangler/
node_modules/
dist/

# OS
.DS_Store
Thumbs.db

# Large data artifacts (bulk processing)
*.json
!package.json
!package-lock.json
!tsconfig.json
*.zip

# Large SQL artifacts at root (bulk imports, dedup scripts)
/*.sql

# Transformed data
transformed/

# Notes (large reference docs)
_notes/

# Claude internals
.claude/

# HobBot is its own repo
HobBot/

# Local CDN mirror (large)
cdn/

# Wildcard source dumps (large)
wildcards/

# Script output / scratch logs
scripts/output/

# Scratch previews and audit dumps at repo root
/*.txt
/audit-*.*
/cc-brief-*.md
/aesthetic-mass-*.*
